# 확인 필요 — 값이 갈리거나 판단이 남은 것

자료를 읽다가 **한쪽으로 정하지 못한 것**을 모아둡니다.
값을 쓸 때 이 문서를 먼저 보고, 정해지면 그 줄을 `[정함]` 으로 바꿉니다.

2026-09-15 작성.

---

## 1. 농사로 자체가 어긋나는 값 — 3건

같은 작물의 같은 표인데 **API 응답과 첨부 문서의 값이 다릅니다.**
우리 파싱 문제가 아니라 농사로 쪽 불일치입니다. 양쪽 원문을 직접 열어 확인했습니다.

### 1-1. ⚠ 수박 만생종 — 열매달림 후 일수

```
API (workScheduleDtl 의 cn)        만생종  40~45일 / 1,200℃
첨부 (수박 농작업일정.hwpx)          만생종  45~50일 / 1,200℃
```

나머지 세 줄은 양쪽이 완전히 같습니다.

```
소과종 35~38 / 750    조생종 38~40 / 800    중생종 40~45 / 1,000
```

**첨부(45~50)를 채택합니다.** 근거 셋.

```
① 계단이 이어진다      35~38 → 38~40 → 40~45 → 45~50
                    API 판은 중생·만생이 둘 다 40~45 라 숙기를 나눈 뜻이 없어진다
② 적산온도와 맞는다    1,200 ÷ 42.5일 ≈ 28.2℃/일   ← 같은 밭에서 불가능
                    1,200 ÷ 47.5일 ≈ 25.3℃/일   ← 자연스럽다
                    (중생은 1,000 ÷ 42.5 ≈ 23.5℃/일)
③ 첨부가 더 자세하다   CLAUDE.md 의 방침이고 이번 대조에서도 표 수가 6~7배였다
```

**⚠ 이것은 우리 추론이지 농진청 확인이 아닙니다.**
`crop_variants` 에 넣을 때 이렇게 적습니다.

```
days_to_harvest = 47          (45~50 의 중앙)
confirmed = N
source = "농작업일정 첨부 '수박 농작업일정.hwpx' 성숙기 판정표.
          API 본문(workScheduleDtl)은 40~45 로 달라 첨부를 채택함"
```

→ **할 일** : 농사로 웹 화면이나 농진청 자료로 어느 쪽이 맞는지 확인

### 1-2. 마늘 퇴비 — 1,200 vs 2,000 kg/10a

```
API    퇴비 1,200 (밑거름 1,200)
첨부    퇴비 2,000 (밑거름 2,000)
```

같은 `비료주기` 표인데 **퇴비 줄만** 다릅니다. 요소 54 · 용과린 39 · 황산칼리 40 ·
석회 200 은 양쪽이 같습니다.

**판단 보류.** 비료량은 지금 스키마(`crops`·`crop_variants`·`crop_stages`)에 들어가지
않으므로 당장 쓸 일이 없습니다. 시비 안내를 붙이게 되면 그때 정합니다.

---


### 1-3. [정함] 주간농사정보 합본 한계온도표의 마지막 칸은 쓰지 않는다

2026-09-16 발견 · 같은 날 정함.

```
합본 과채류    0 / -1~-2 / 0~2 / 0~2 / 0~2 / 0~2
  = 재해예방정보 **과채류 한계온도표**(냉해)와 한 칸도 다르지 않다
합본 엽근채류   0 / -6 / -5 / -10 / -5 / 0
  = 엽채류 한계온도표와 같은 칸 모양인데 **배추만 -6** (생육및장애표 = 동해 값)
```

칸 이름은 '동해온도' 하나인데 **원판 둘 중 어느 쪽인지 일관되지 않는다.**
한쪽으로 몰면 다른 쪽이 깨진다.

```
전부 냉해로 보면   딸기 0 은 맞고  배추 -6 이 냉해 -8 과 어긋난다
전부 동해로 보면   배추 -6 은 맞고  딸기 0 이 동해 -5 와 어긋난다
```

농진청이 합치면서 배추 값을 바꿔 적은 것으로 보이나 어느 쪽이 맞는지 알 수 없다.

**[정함] 그 칸은 규칙으로 만들지 않는다.** 원판(재해예방정보)이 이미 §B-4 대로
동해 −6 · 냉해 −8 을 갈라 담고 있다. 합본판에서는 단계별 저온(모기를때·꽃눈생기기전)만 담는다.

값을 잃는 것이 아니다 — `out/원본별/주간농사정보/tables.csv` 에 원문이 그대로 있다.
실린호(근거 세기)만 6개 호 덜 세어진다.


## 2. 값을 고르는 규칙이 걸린 것

### 2-1. 가지 `base_temp` — 자료마다 6~7℃ 차이

```
Paredes et al. (2025)   10 · 11
MRCC                    15.6
국내(농작업일정 본문)       17      "17℃ 이하에서는 생육이 정지됨"
Safe-farm 더미           12.0    출처 없음
```

A-1 규칙 ① 은 "국내 1차자료 우선" 이지만, **단서로 "논문 범위에서 3℃ 이상 벗어나면
보류"** 를 붙였습니다. 가지가 유일하게 여기 걸립니다.

국내 문장이 `base_temp`(회귀 절편)가 아니라 **"생육 정지 체감 온도"** 를 말한 것일
수 있습니다. GDD_작업인계.md §5-4 에도 미결로 올라 있습니다.

→ **할 일** : 10~11 을 쓰고 `confirmed=N` 으로 둘지, 국내 17 을 쓸지 정하기

### 2-2. 상추 1,400~1,700℃ — 목표값이 아님

```
원문   "꽃대신장은 적산온도 1,400~1,700℃에서 나타남"
```

**추대(꽃대신장) 경보값**이지 수확 목표가 아닙니다. 상추는 그 전에 거둡니다.
`sumtemp.py` 가 `쓸모=다른용도` 로 표시하도록 고쳤습니다.

→ **할 일** : `disaster_rules` 에 "상추 추대 경보" 로 옮길지 정하기.
   목표 적산온도는 따로 역산해야 합니다.

---

## 3. 자료가 없어 채우지 못하는 것

### 3-1. 숙기 3분할 — 실측 근거가 5작물뿐

```
근거 충분   논벼 371품종 · 감자 46 · 양파 40 · 배추 3품종(조·중·만 각 1)
           수박은 품종 분류는 없으나 **숙기별 목표 적산온도 표**가 있어 가능

갈래 하나   상추(중생 6) · 마늘(만생 4) · 고추(극조생 1)
자료 없음   무 · 파 · 당근 · 시금치 · 가지
```

무·파는 품종요약서가 **F1 육성용 중간모본**이라 숙기 개념 자체가 없습니다.
당근·시금치·가지는 품종정보 서비스에 품종이 아예 없습니다(농작업일정에는 있음).

→ **할 일** : 근거 없는 9작물을 MID 한 행으로 둘지, 생육일수 비례로 추정할지 정하기
   (`GDD_작업인계.md` §1-6 은 "없는 걸 지어내지 않는다" 로 정해둠)

### 3-2. 배추·파 `base_temp` — 논문에 없음

```
배추   근연 Turnip(B. rapa) 3.6·4 / Cabbage(B. oleracea) 4·4.4
파     없음
```

A-1 규칙 ④ 가 "근연 추정 4~5, confirmed=N" 입니다. 파는 근연도 없습니다.

→ **할 일** : 일본 자료(NARO)를 볼지 정하기. 단 일평균 정의가 달라 그대로 못 씁니다
   (`GDD_작업인계.md` §4-3)

### 3-3. 고랭지 작형 적합 표고 — 벼만 있음

```
벼 기계이앙   중모 중북부 300 / 남부 350m 이하 · 어린모 200 / 250m 이하
벼 직파      중북부 100m 이하 / 남부 200m 이하
배추·무 고랭지  없음
```

→ **할 일** : 고랭지 작형을 추천할 때 해발 기준이 없으면 아무 데나 추천하게 됩니다

---

## 4. 도구에 남은 구멍

### 4-1. 검수 ⑦ 격자 불일치 — 오탐 93%

열 번호로 맞대서 30건이 나왔는데 **진짜는 2건**이었습니다.
나머지는 병합 칸이 밀린 것(파·배추), 글머리표 `ㅇ` 차이(벼), 표 구조가 다른 것
(감자·유채·참깨)입니다.

→ **할 일** : 열 번호가 아니라 **행 이름**으로 맞대도록 고치기.
   손으로 그렇게 해보니 30 → 2건이 됐습니다.

### 4-2. `기준온도후보.csv` 가 검수 대상에서 빠짐

`값최소`·`값최대` 라는 칸 이름을 `kind_of()` 가 온도로 못 알아봅니다.
16행짜리라 눈으로 다 읽었지만, 늘어나면 놓칩니다.

### 4-3. API 에 표가 아예 없는 작물

```
가지 · 토마토,방울토마토    API 표 0개 · 첨부 6개
```

**첨부가 유일한 출처**입니다. 첨부를 안 받았으면 표 데이터가 통째로 없었습니다.
첨부는 API 보다 표가 평균 6~7배 많습니다(감자 1 vs 8, 당근 1 vs 7, 마늘 1 vs 7).

### 4-4. `|` 로 이어진 작물 값 102행

병해충 경보는 한 행이 여러 작물에 걸립니다.

```
마늘 | 양파              58행
방울토마토 | 고추          36행
방울토마토 | 고추 | 상추      6행
방울토마토 | 상추           2행
```

`재해규칙_병해충경보.csv` · `병해충발생정보_경보.csv` 의 `_선정작물` 칸입니다.
행을 쪼갤지, `crop_id` 를 다대다 관계로 뺄지는 스키마 결정입니다.

→ **할 일** : 적재 전에 반드시 정할 것. 지금 그대로 넣으면 `마늘 | 양파` 라는
   없는 작물이 생깁니다. `로드맵_아카이브.md` §8-3 에 같은 형태가 이미 실수로 기록돼 있습니다.

### 4-5. [처리함] 온도 추출률 38% — 문장형을 통째로 놓쳤다

`원본검수_2026-09-15.md §10`. 본문 974개 중 `첨부_온도.csv` 에 372행만 들어갔습니다.
**본문에 온도가 있는데 CSV 0행인 작물이 43종**이고 감자·옥수수가 여기 들었습니다.

```
감자 농작업일정.txt:100   ❍ 씨감자 싹은 5℃ 이상에서 자라기 시작함    ← CSV 에 없었음
```

추출기가 `발아적온 : 15~20℃` 같은 `항목 : 값` 꼴만 잡고 `❍ ~ 함` 문장을 못 봤습니다.
정정 요청서 ⑥ 이 나온 원인입니다.

**2026-09-16 — `보정/온도.py` 로 처리했습니다. 372 → 870건(새로 466). 감자 0 → 12행.**
`attach.py` 는 건드리지 않았습니다. 이미 검증된 372행이 흔들리면 안 되기 때문입니다.

→ **남은 할 일** : `보정/결과/온도_전체.csv` 의 `쓸모=확인필요` **581건**은 개수만 센
   것입니다. 어느 것이 `base_temp` 인지는 원문을 읽어야 압니다. 본선 작물부터 보십시오.

### 4-6. [반은 처리함] 수박 숙기별 적산온도

```
적산온도_적산온도.csv   소과종 750 · 조생종 800 · 중생종 1,000 · 만생종 1,200   있었음
crops_초안.csv        수박 target_sum = 800~1,000 하나뿐                     숙기 구분 없었음
```

같은 표의 `열매달림 후 일수`(35~38 / 38~40 / 40~45 / 45~50)도 빈 채였습니다.
두 칸짜리 표에서 적산온도만 가져왔던 것입니다.

**2026-09-16 — `보정/반영.py` 로 `crop_variants_초안.csv` 4행을 만들었습니다.**
적산온도와 일수를 함께 담았습니다. 만생종은 `confirmed=N` 입니다(§1-1 때문).

→ **남은 할 일** : 수박을 정식으로 숙기 분할에 넣을지 **정하기.**
   `GDD_작업인계 §1-6` 은 "숙기 3분할은 벼·감자·양파만" 이라고 했습니다.
   수박은 원문에 숙기별 표가 있으니 넷으로 나뉩니다. 규칙을 고칠지, 수박만 예외로 둘지
   정해야 `crop_variants_초안.csv` 를 정본으로 쓸 수 있습니다.

### 4-7. [처리함] 추출 찌꺼기 두 가지

```
hwp(OLE) 제어코드가 한자로   23,538곳   漠杳 氠瑢 浴瑣 …
                                      95605_대영.txt 는 76%가 찌꺼기였다
HTML 엔티티 미해제           865곳     &#8228; 802 · &#160; 165 …  품종정보 CSV 6개
생짜 마크업                 4,155곳    <SPAN STYLE="font-size:15pt;">
```

**2026-09-16 — `보정/찌꺼기.py --고침` 으로 1,197파일을 씻었습니다.**
95605_대영.txt 는 91,927자 → 9,560자, 한자비율 76% → 0% 가 됐습니다.

⚠ 규칙을 고칠 때 조심할 것이 있습니다. "ASCII 로 되돌려 소문자면 찌꺼기" 로 하면
**진짜 한자가 지워집니다.**

```
正東 → "ckqg"   ← 주간농사정보의 `정동(正東)에서`. 하마터면 지울 뻔했다
漠杳 → "o gs"   ← 이쪽만 진짜 찌꺼기
```

지금은 알려진 토막 목록으로만 지웁니다. 새 자료를 받으면 `python 찌꺼기.py --낯선` 으로
빠진 토막이 없는지 한 번 훑으십시오.

### 4-8. 표 대조를 행 이름으로 다시 한 결과 — 새 불일치

`확인필요 §4-1` 이 말한 "열 번호 대신 행 이름으로" 를 `보정/대조.py --격자` 로 했습니다.
801행 중 **76건**이 다릅니다(열 번호식은 오탐이 93% 였습니다).

이 방법이 **§1-1 수박 만생종과 §1-2 마늘 퇴비를 스스로 다시 찾아냈습니다.** 검산이 됩니다.

새로 나온 것:

```
비트  칼리   API 25      vs  첨부 24
풋콩  비료   API 용과린    vs  첨부 용성인비
```

→ **할 일** : `보정/결과/대조_표_행이름.csv` 76건을 훑어 진짜 불일치만 골라내기.
   비트·풋콩은 등록 작물이 아니라 급하지 않습니다.

### 4-9. ⚠ 라이선스 — 공공누리 마크가 **없다**

`출처.txt §6` 갱신. 농사로 저작권 정책은 이렇게 말합니다.

```
공공누리 마크가 붙은 자료   유형별 조건에 따라 별도 허락 없이 이용
마크가 없는 자료           사이트 관리자의 사전 승인 필요
```

내려받은 원본 6,220개를 전부 훑었습니다. **공공누리·KOGL 문구가 든 파일은 0건입니다.**

→ **할 일** : 농진청 농사로에 직접 문의. ① 공공누리 유형 ② 상업 서비스 탑재 가부
   ③ 첨부문서(hwp/pdf)도 같은 조건인지. **서비스를 외부에 내기 전에 반드시 정할 것.**
   내부 개발·검증은 문제없습니다.


---

## 5. 숙기 `조중생` 을 어느 쪽으로 접나

2026-09-16 추가. 4단계(품종정보)를 돌리면서 드러났습니다.

원문이 `조중생` · `조·중생종` · `조중숙성` 이라고 적은 품종이 있습니다.
**조생도 중생도 아닙니다.** 3분류로 접을 때 어느 쪽으로 보낼지 정해야 합니다.

```
숙기가 '조중생' 으로 남은 품종   18건   본선 작물은 6건 (상추 4 · 양파 2)
                                    나머지 12건은 백합·국화·배 등 등록 밖 작물
```

(raw 만 센 것입니다. 첨부 본문까지 돌리면 늘어납니다.
 정확한 수는 `pipeline/out/원본별/품종정보/varieties.csv` 의 `숙기가능성` 칸으로 셉니다.)

### ★ 농사로 자신은 '중생' 으로 본다 — 감자 13건이 증거

감자는 46품종 중 42품종에 숙기 **코드**가 붙어 있어 조중생이 겉으로 안 드러납니다.
그런데 코드와 주요특성을 나란히 놓으면 이렇습니다.

```
코드 = 중생   ·   주요특성 = "지상부 숙기는 조중숙성이며"     13건
코드 = 조생   ·   주요특성 = 조중생                          1건
```

장원 · 추원 · 아란 · 금나루 · 골든볼 · 수지 · 은선 · 남선 · 서홍 · 고운 · 하령 · 추영 · 추동.

**농진청이 조중생 품종에 중생 코드를 붙였습니다.** 13 대 1 입니다.
옛 추출기(`보관/품종정보/variety_stage.py`)가 `조중생 → 중생` 으로 접은 것과 같습니다.

### ⚠ 확정표 §D 수치는 이미 '중생' 으로 접혀 있다

확정표 §D 의 숙기 분포는 그 옛 추출기가 만든 `품종_숙기.csv` 에서 나왔습니다.

```python
if word in ("조중생", "중만생", "중만숙"):
    return "중생" if word == "조중생" else "중만생"
```

그래서 확정표에는 조중생이 아예 안 보입니다.

```
확정표 §D-2 ①  "상추(중생 6)"      ← 이 6건이 전부 조·중생종이다
확정표 §D-1    양파 중생 9          ← 실제로는 중생 7 + 조중생 2
```

새 추출기(`pipeline/src_variety.py`)는 접지 않고 `조중생` 그대로 담고,
`숙기가능성` 칸에 `조생|중생` 만 적습니다(CLAUDE.md "원본 층은 접지 않는다").

### 갈림길

```
① 중생으로 접는다   농사로 코드 13:1 · 옛 추출기 · 확정표 §D 현재 값. 셋이 같다
② 조생으로 접는다   사용자 1차 의견(2026-09-16) — 위 증거를 보기 전이다
③ 따로 둔다        3분류로 안 가고 갈래를 늘린다
```

→ **할 일** : 정하고 나서 ⓐ 확정표 §D 에 접기 규칙 한 줄 ⓑ §D-1·§D-2 수치에
   조중생 몇 건이 섞였는지 註 ⓒ `build.py` 에 규칙 반영.
   **`src_variety.py` 는 안 고칩니다** — 원본 층은 세밀한 채로 두기로 했습니다
   (2026-09-16 결정, CLAUDE.md 핵심 설계 원칙).

### 곁가지 — 숙기충돌 39건

`varieties.csv` 의 `숙기충돌=Y` 는 코드와 주요특성이 어긋난 품종입니다.
위 조중생 13건 말고도 26건이 더 있습니다(본선 작물 13건).

```
중만생 vs 만생 6 · 중생 vs 조생 6 · 극조생 vs 조생 3 · 만생 vs 중만생 2 …
```

→ **할 일** : `build` 가 코드를 우선하도록 두되, 이 26건은 사람이 한 번 훑을 것.

---

## 6. [정함] 숙기를 셋으로 갈랐는데 `days_to_harvest` 가 안 갈린다

> **정함 (2026-09-16)** — 지금 고치지 않는다. 저쪽에서 `gdd_target` 을 계산해
> 채울 때 **같이 가른다.** 까닭은 아래 '곁가지' 절.

`build.py` 가 벼·감자·양파·수박을 조·중·만생 세 행으로 나누는데, 세 행의
`days_to_harvest` 가 **똑같습니다.** 작형표에 작물당 한 값밖에 없어서 그대로 복사됩니다.

```
감자  EARLY 106 · MID 106 · LATE 106
벼    EARLY 137 · MID 137 · LATE 137
양파  EARLY 223 · MID 223 · LATE 223
수박  EARLY 101 · MID 101 · LATE 101
```

조생종이 만생종과 같은 날 여문다는 뜻이 되어 **틀린 말을 하고 있습니다.**

### 수박은 1차 자료에 답이 있습니다

`숙기원문` 칸이 이미 착과 후 일수를 갈라 담고 있습니다.

```
EARLY  소과종 (착과 후 36일) + 조생종
MID    중생종 (착과 후 42일)
LATE   만생종 (착과 후 47일)
```

MID 대비 −6일 · +5일입니다. 101일에 얹으면 95 / 101 / 106 이 됩니다.

### 왜 지금 안 고치나

숙기를 가르는 **진짜 값은 `gdd_target`** 인데 19행 전부 비어 있습니다(저쪽에서 계산).
`days_to_harvest` 는 그 뒤를 따라가는 값입니다. 지금 일수만 손대면 —

```
gdd_target 이 채워질 때 또 한 번 만져야 한다   → 같은 자리를 두 번 고친다
둘이 어긋나면 어느 쪽이 맞는지 알 수 없다      → 조생 95일인데 target 은 중생값
```

그래서 **`gdd_target` 을 채우는 작업과 한 번에** 가릅니다.

### 그때 할 일

```
1  수박   숙기원문에 답이 있다. 착과 후 36 / 42 / 47일 = MID 대비 −6 / +5
         101일에 얹으면  EARLY 95 · MID 101 · LATE 106
2  벼·감자·양파
         숙기별 일수의 1차 자료가 **없다.** 지어내지 말 것.
         gdd_target 에서 역산하거나, 못 하면 MID 외를 비운다
3  고칠 곳  pipeline/build.py 의 crop_variants()
         값을 정하는 것은 확정표 §D 다
```

⚠ **그때까지는 세 행이 같은 값입니다.** 알고 두는 것이니, 이 상태를 보고
  "조생과 만생이 같은 날 여문다" 고 읽지 마세요.

⚠ `gdd_target` 은 저쪽에서 NOT NULL 입니다. nullable 로 바꾸거나 채운 뒤 적재해야 합니다.
