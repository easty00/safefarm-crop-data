# 세이프팜 (SafeFarm) — 작물 생육 추천 시스템

노지 농민 대상 생육 상태·관수/방제 시기 추천 서비스. 위성·기상 데이터와 작물 데이터를 결합해
LLM(Claude)이 사람 말로 조언을 만들어주는 구조.

## 이 작업 공간의 범위

**이 세션(및 이 폴더)에서는 작물 관련 데이터(농사로 API + 적산온도 계산 로직)만 다룬다.**
기상·위성 데이터는 다른 담당자가 별도로 진행 중이므로 건드리지 않는다.

계산 파라미터의 정본은 `GDD계산관련/작물_확정표.md`, 그 근거는 같은 폴더의
`GDD_작업인계.md` 다. 값을 만지기 전에 둘 다 읽을 것. 미결 항목은 `확인필요.md`.

## 실행

테스트·빌드·린트가 없다. **데이터 파이프라인이라 스크립트를 순서대로 돌리는 게 전부다.**

```bash
# 만들기
python pipeline/extract.py      # 원본 다섯을 돌려 모은다 → out/mid_*.csv
python pipeline/build.py        # 중간 CSV + 확정표 상수 → 스키마 4종
python pipeline/verify.py       # 빈칸·범위·제약 검사

# 원본 하나만 다시 뽑기 (산출물은 out/원본별/<이름>/ 에 남는다)
python pipeline/src_farmwork.py     # 농작업일정
python pipeline/src_disaster.py     # 재해예방정보
python pipeline/src_week.py         # 주간농사정보
python pipeline/src_pest.py         # 병해충발생정보  ⚠ PDF 57개라 3분쯤
python pipeline/extract.py --merge  # 다시 안 돌리고 있는 것만 모은다

python pipeline/extract_pest_alert.py   # 병해충 경보 → out/pest_alert.csv (따로)

# 도구 (단독 실행)
python feature/hwp_extract.py <폴더>    # hwp/hwpx → txt
python feature/ocr.py <폴더>            # 스캔 PDF → txt

# 수집 (이미 끝났다. 구멍이 났을 때만)
python 원본/<서비스>/<수집>.py
```

필요한 패키지 — 전부 설치 확인됨 (Python 3.13).

```
pypdf      PDF 텍스트
olefile    hwp (OLE)
pymupdf    스캔 PDF 렌더
winocr     Windows 내장 OCR (tesseract·torch 불필요)
requests   수집용
```

⚠ **Windows cp949 라 한글 출력이 깨진다.** 스크립트를 돌릴 때 붙인다.

```bash
PYTHONIOENCODING=utf-8 python pipeline/extract.py
```

⚠ **스크립트는 사람이 돌린다.** 오래 걸리는 것은 `Start-Process` 로 독립 프로세스로 띄우고
로그만 확인한다. 세션의 자식으로 돌리면 세션이 끝날 때 같이 죽는다.

## 데이터 흐름 — 큰 그림

```
원본/<서비스>/raw/      API 가 준 XML      시기 · 목록 · 코드
원본/<서비스>/첨부/      hwp·hwpx·pdf      온도 · 적산온도 · 재해기준 · 품종특성
        │
        │  pipeline/extract.py   ← 등록 작물만 골라 읽는다
        ▼
   중간 CSV
        │
        │  pipeline/build.py     + GDD계산관련/작물_확정표.md 의 상수
        ▼
   pipeline/out/    crops.csv · crop_variants.csv · crop_stages.csv · crop_disaster_rules.csv
        │
        ▼
   Safe-farm/ai-service (형제 폴더) 의 같은 이름 테이블로 시딩
```

**어느 값이 어디에 있는지가 갈린다.**

```
raw/ 에만 있는 것    파종·수확 순(旬) 단위 시기   ← workScheduleEraInfoJsonLst 응답
                   품종 목록 · 숙기 코드 · 문서 목록
첨부/ 에만 있는 것   생육 온도 · 적산온도 목표값 · 재해 한계온도 · 품종 특성문
어느 쪽에도 없는 것   base_temp 12/14 · upper_temp 11/14
                   → 논문(Paredes 2025)과 사람 판단. 확정표에 상수로 박혀 있다
```

⚠ **`첨부/` 하위 구조가 서비스마다 다르다.** 하나로 가정하고 훑으면 0건이 나온다.

```
농작업일정     첨부/*.hwpx                     평평함. 등록 작물분
품종정보       첨부/<작물>/*                   작물별. 13작물 655개
                                          ⚠ 확장자는 .hwp·.pdf 인데 속이 다섯 갈래다
                                          hwp 387 · pdf 206 · hwpx 59 · 사진 2 · 한글3.0 1
병해충발생정보  첨부/<연도>/*.pdf                연도별
주간농사정보   첨부/<연도>/*.hwp|hwpx|pdf        연도별
재해예방정보   첨부/<계열>/<연도>/*             ★ 두 층. 고온해·월간회보·저온해·홍수해
```

⚠ **뒤 셋은 문서 하나에 작물 수십 개가 섞여 있어 파일 단위로 못 줄인다.**
`extract.py` 가 읽으면서 등록 작물을 걸러야 한다.

## 작물

**목록과 수를 여기에 적지 않는다.** 정본은 `pipeline/crops.py` 의 `CROPS` 하나다.

```bash
python pipeline/crops.py       지금 등록된 작물·문서·본선 여부를 찍는다
```

⚠ 예전에는 "14작물" 이라는 말이 20개 파일에 70여 번 박혀 있었다. 작물을 줄이자
**한꺼번에 전부 틀린 말이 됐다.** 수와 목록은 코드가 만들고 문서는 가리키기만 한다.

```
본선      제품에 넣는 작물. mid_*.csv 의 '본선' 칸이 Y 가 된다
확인용     근거 추적용으로만 뽑는다. '본선' 칸이 빈칸
문서       cntntsNo 하나. 벼·배추·무·마늘은 작형 갈래로 문서를 둘씩 갖는다
```

작물을 더하려면 `pipeline/add_crop.py` (사용법.md §1.6).
빼려면 `CROPS` 에서 그 줄을 지우고 **왜 뺐는지를 그 아래 주석에 남긴다** — 지금 그렇게 돼 있다.

**작형을 반드시 지정한다** — 이름만 쓰면 시설 작형이 섞인다.

```
고추 → 고추(보통재배)          수박 → 작형 '노지재배'
방울토마토 → '조숙재배' 또는 '고랭지재배'
```

## 핵심 설계 원칙

- **판단은 코드, 문장은 Claude.** 적산온도·생육단계·경보 판정은 전부 결정론적 코드로 계산하고,
  Claude는 그 결과를 사람이 읽을 문장으로 바꾸는 역할만 한다. 입력에 없는 수치를 지어내지 않는다.
- **작물 마스터 데이터는 실시간 조회 대상이 아니다.** 배포 시 1회 시딩 + 연 1회 배치 갱신.
  사용자 요청 시점에 농사로 API를 호출하지 않는다.
  (예외: 재해예방정보 — 월간회보는 월 1회, 저온해·고온해 보고는 계절 중 주 1회)
- **작물은 전수 수집 후 선별한다.** 미리 몇 종으로 좁히지 않는다. 수집 → 분류 → 쳐내기 순서.
- **원본 층은 접지 않는다. 합치는 것은 build 가 한 번만 한다.** 원문이 여섯 갈래로
  말하면 여섯 갈래로 담는다. 추출기에서 미리 접으면 같은 규칙이 두 곳에 생기고,
  규칙이 갈리면 어느 쪽이 정본인지 모르게 된다 — 이 프로젝트가 이미 겪은 사고다.
  세밀한 것은 나중에 합칠 수 있지만, 접어버린 것은 되돌릴 수 없다.
- **모든 숫자에 `source`와 `confirmed`를 붙인다.** `confirmed=Y`는 1차 자료 확인,
  `N`은 역산·추정값. "이 숫자 어디서 났냐"에 정직하게 답하기 위한 장치다.

## 적산온도 계산 — 두 개의 독립된 축

⚠ 축 1 과 축 2 는 독립이다. 섞어서 한 이름으로 부르지 말 것.
결론만 먼저: **누적은 ③ 하나, 상한은 `upper_temp` 컬럼.** 근거와 개념 구분은 아래.

```
축 1  누적 방식     ③ 유효적산온도(GDD) Σ(일평균 − 기준온도) 하나로 통일.
                   sum_method 컬럼은 만들지 않는다 (GDD_작업인계 §1-3).
                   ①②는 원문 해석에만 쓴다 — 아래 함정 참고.

                   ① 단순적산 Σ(일평균)
                   ② 적산온도(관행) Σ(일평균), 기준온도 이상인 날만 — 빼지 않음
                   ③ 유효적산온도(GDD) Σ(일평균 − 기준온도) — 뺌

축 2  고온 상한     FAO56rev 식 (2) 의 Tupper 클램프
                   → crops.upper_temp 컬럼 (nullable). 추가하는 컬럼은 이것 하나뿐.
```

"표준 GDD는 상한=∞인 특수케이스"라는 논리는 **③ 안에서만** 성립한다. ②와 ③의 차이는 그 논리로
통합되지 않는다. 그래서 ②로 적힌 원문 목표값을 ③ 으로 그대로 옮기면 안 된다.

**함정**: 농진청의 벼 목표값(출수 후 1,100~1,200℃)은 **② 단위**다. 검산 50일×22℃≈1,100 ✅ /
base 7을 뺀 ③으로는 750 ❌. GDD 목표값으로 그대로 쓰면 안 된다.

한 작물·한 화면 안에서 방식을 섞지 않는다. 작물 간에도 섞지 않는다 — 전부 ③ 이다.
농민 화면에는 계산 방식을 노출하지 않는다.

### ⚠ 한계온도와 기준온도는 다른 숫자다

재해예방정보에서 작물별 **한계온도**를 대량으로 확보했다(과채류 6종·엽채류 6종·과수 7종·벼 10단계).
쓸모가 크지만 **적산온도 계산의 `base_temp` 로 그대로 쓰면 안 된다.**

```
한계온도   그 온도 아래/위로 가면 "피해가 난다"        → 재해 경보 판정용 (crop_disaster_rules.csv)
기준온도   그 온도 아래에서는 "생육이 진행되지 않는다"  → 적산온도 계산용 (crops.base_temp)
적온      가장 잘 자라는 온도                        → 어느 쪽도 아님. 계산에 쓰지 말 것
```

예: 배추 냉해온도 -8℃는 배추가 -8℃까지 자란다는 뜻이 아니다. 섞으면 적산이 전부 어긋난다.

## 숙기 — 원본은 여섯 갈래다

`crop_variants.maturity_type` 의 유일한 출처가 품종정보다. 다른 원본에는 품종이 없다.
**원본 층(`varieties.csv`)은 원문이 말한 낱말 그대로 둔다.**

```
극조생 · 조생 · 중생 · 중만생 · 만생 · 조중생
```

3분류(조생·중생·만생)로 접는 것은 **build 의 일**이다. 접기 규칙의 정본은 확정표 §D.

⚠ **`조중생` 은 확정표에 없는 갈래다.** 접을 때 어느 쪽으로 보낼지 아직 안 정했다.
`varieties.csv` 의 `숙기가능성` 칸에 `조생|중생` 로만 적어둔다 (`확인필요.md §5`).

근거는 **중생** 쪽이 무겁다. 농진청이 조중생이라고 적은 감자 품종에 붙인 숙기 코드가
**중생 13 · 조생 1** 이다. 옛 추출기(`보관/품종정보/variety_stage.py`)도 중생으로 접었고
확정표 §D 수치가 그 결과다.

### 숙기는 세 곳에 따로 있다

코드만 보면 16%, 본문까지 읽어야 절반쯤 나온다. 어디서 났는지 `숙기근거` 칸에 적는다.

```
코드      mtrtSeCode (220xxx)     농사로가 부여한 것. 가장 믿을 만하다
주요특성   mainChartrInfo 문장      목록 응답 안에 있다. 가장 많이 나온다
본문      첨부 품종요약서           코드도 주요특성도 빈 품종의 마지막 갈래
풀어씀     "숙기가 빠른" → 조생      낱말이 아니라 **해석**이다. 근거 칸에 따로 적는다
```

### ⚠ 숙기말과 헷갈리는 것 넷

전부 실제로 틀린 값이 들어갔던 자리다.

```
작형      "2011년 조숙재배"       ← 작형이지 숙기가 아니다. 수박 3품종이 조생이 됐었다
대비품종   "대비품종 수미 조생"     ← 이 문서의 주인공이 아니다. 남의 숙기다
가운뎃점   "조․중생종"            ← U+2024. 공백으로만 이으면 뒤쪽 '중생'만 걸린다
차례      중만생 → 중생 순서      ← 짧은 것을 먼저 찾으면 중만생이 전부 중생이 된다
```

## 농사로 API 사용법

Base URL: `http://api.nongsaro.go.kr/service/{서비스명}/{오퍼레이션명}?apiKey=...`

- 서비스명은 폴더명과 1:1 대응. **오퍼레이션명은 추측하지 말 것** — 서비스명이 틀려도 오퍼레이션명이
  틀려도 똑같이 `resultCode 13`이 나와서 구분이 안 된다. `API매뉴얼/<서비스>/샘플소스/...` 또는 같은 폴더의 매뉴얼 txt 를 먼저 열어볼 것
- 응답은 XML, 값은 `<![CDATA[...]]>`로 감싸져 있음
- `farmWorkingPlan`(품목별관리매뉴얼)은 `farmWorkingPlanNew`의 구버전이다. New를 쓸 것
- **첨부파일이 본문보다 상세하다.** 농작업일정 176건의 첨부 hwpx에는 API HTML에 없는 환경관리
  온도·수분장력·생육 일수가 들어있다. `fileDownUrlInfo`로 인증 없이 바로 다운로드된다
- **파일 다운로드 주소는 서비스마다 다르다. 하나로 통일되어 있지 않다**

```
농작업일정      fileDownUrlInfo        (암호화 토큰 ep= 가 응답에 들어있음)
품종정보        atchFileLink           (atis.rda.go.kr 로 직접)
주간농사정보    downUrlList            ⚠ '|' 로 이어진 판본 목록. fileName 과 짝을 맞춰 나눌 것
재해예방정보    아래 서블릿을 쓸 것
```

- ⚠ **재해예방정보에서 `{rtnFileCours}/{rtnStreFileNm}` 정적 경로를 쓰지 말 것.**
  `.pdf`·`.hwp` 는 되지만 **`.hwpx` 는 서버가 HTTP 400 으로 막는다.** 파일이 없어서가 아니라
  확장자 때문이다(성공한 .hwp 주소의 확장자만 바꿔도 400). 2023~2024년 첨부가 대부분 hwpx라
  이 경로로는 112건 중 92건이 실패한다. 대신 농사로가 자기 사이트에서 쓰는 경로를 쓴다.

```
https://www.nongsaro.go.kr/portal/contentsFileDownload.do
    ?cntntsNo={cntntsNo}&fileSeCode={rtnFileSeCode}&fileSn={rtnFileSn}
```

  세 값 모두 목록 응답에 원래 들어있다. 암호화 토큰이 필요 없다.

## 문서 파싱 도구

**확장자를 믿지 말 것. 파일 앞 4바이트로 형식을 판정한다.** 이름과 내용이 어긋난 파일이
실제로 있다(`.pdf` 이름의 JPEG 3건, `.pdf` 이름의 hwpx). 이름을 믿으면 판독이 조용히 실패한다.

| 앞바이트 | 형식 | 방법 |
|---|---|---|
| `%PDF` | PDF | `pypdf`. 쪽 표시를 넣어두면 `source`에 쪽 번호를 적을 수 있다 |
| `PK` | hwpx (ZIP+XML) | `Contents/section*.xml` 파싱. 글자가 가장 깨끗하다 |
| `D0 CF 11 E0` | hwp (OLE) | `olefile` + `zlib(-15)` + `HWPTAG_PARA_TEXT`(67) |
| `HWP ` | **한글 3.0 구형식** | 2바이트 리틀엔디안 → `johab` 변환. 건져내기 수준 |
| `FF D8 FF` 등 | 사진 | 글자가 없는 게 맞다. 실패가 아니라 따로 센다 |

- `feature/hwp_extract.py` — hwp/hwpx → txt. `python hwp_extract.py 폴더경로` (필요: `pip install olefile`)
  단 한글 3.0(`HWP `)과 바이트 판정은 들어있지 않다. 각 서비스 폴더의 스크립트에 있다
- PDF는 `pypdf`로 텍스트 추출 (설치 확인됨). Read 도구의 PDF 렌더링은 poppler 미설치로 불가
- 표는 **칸 경계를 `|` 로 살려둘 것.** 지우면 숫자가 한 덩어리로 붙어 못 읽는다
- 본문은 **정규식으로 거르지 말고 전문을 저장한 뒤 통독할 것.** 걸러 읽었다가 가장 값진
  자료 셋(벼 한계온도표·가루쌀 적산온도·침관수 감수율표)을 전부 놓친 적이 있다
- ⚠ **같은 실수가 온도 추출기에 있었다.** `attach.py` 가 `항목 : 값` 꼴만 잡아서 본문
  974개 중 372개(38%)만 CSV 로 갔다. `❍ 씨감자 싹은 5℃ 이상에서 자라기 시작함` 같은
  문장형을 통째로 놓쳤다. `feature/temperature.py` 가 870건으로 늘렸다 (`확인필요.md §4-5`)
- ⚠ **`이상` 이 붙었다고 하한이 아니다.** 뒤에 오는 말이 뜻을 뒤집는다.
  `5℃ 이상에서 자라기 시작` = 하한(base_temp) / `25℃ 이상에서는 생육 정지` = 상한(upper_temp).
  둘을 묶었다가 마늘·양파·시금치의 고온 상한 25℃ 가 base_temp 후보로 올라온 적이 있다
- ⚠ **글자 찌꺼기를 지울 때 진짜 한자를 지우지 말 것.** "ASCII 로 되돌려 소문자면 찌꺼기"
  규칙은 `正東`(→"ckqg")도 지운다. 알려진 제어토막 목록으로만 지운다 (`feature/clean_text.py`)
- 스캔 PDF 는 `pypdf` 로 글자가 안 나온다(폰트 0개). Windows 내장 OCR 을 쓴다
  (`feature/ocr.py`, `pip install pymupdf winocr`). tesseract·torch 가 필요 없다

## 파일 구조

2026-09-16 에 전면 재편했다. **등록 작물만 다루는 구조**로 바꾸고, 전수 수집 시절의
산출물·스크립트는 전부 `보관/` 으로 내렸다.

```
CLAUDE.md          이 문서. 규칙
확인필요.md         값이 갈리거나 판단이 남은 미결 항목. 값 쓰기 전에 볼 것
숙기데이터_확인.md   숙기(조·중·만생) 검증 기록
출처.txt           자료 출처 + 이용조건 미확인 항목
.env              실제 API 키. 커밋 금지, 전체 값 출력 금지

원본/             ★ 입력. 읽기만 한다. 5.3GB
  <서비스>/  <수집>.py + <수집>사용법.md + raw/ + 첨부/
    농작업일정/      farmWorkingPlanNew   raw  74 · 첨부  17 (등록 문서분)
    품종정보/        varietyInfo          raw  26 · 첨부 655
    재해예방정보/     frcDsstrPrevnt       raw  17 · 첨부  69 (2023~2026 · 저온해 뺌 ↓)
    주간농사정보/     weekFarmInfo         raw   2 · 첨부 192 (전수)
    병해충발생정보/   dbyhsCccrrncInfo     raw   5 · 첨부  57 (전수)

  ⚠ 개수는 2026-09-16 기준이다. 세어보려면 —
        find 원본/<서비스>/첨부 -type f | wc -l

  ⚠ **재해예방정보의 `저온해` 계열은 일부러 안 받았다. 구멍이 아니다.**
    목록에는 2023~2025년 42건이 있고 `보관/재해예방정보/결과/파일/저온해/` 에
    받아둔 것도 43개 있다. 그런데 **내용이 과수 5종(사과·배·복숭아·매실·포도) 전용**이라
    등록된 노지 작물이 한 줄도 안 나온다 (43개 전문을 읽어 확인 — 작물+℃ 0줄).
    표 모양은 우리 `crop_disaster_rules` 와 같다(`≤-2.8℃ (주의)` · `≤-5.0℃ (위험)`).
    **나중에 과수를 등록하면 여기가 바로 그 자료다.**
        python 원본/재해예방정보/disaster.py --only 저온해

  raw/   API 가 XML 로 돌려준 것. 시기·목록·코드가 여기 있다
         ⚠ 파종·수확 순(旬) 은 첨부에 없고 workScheduleEraInfoJsonLst 응답에만 있다
  첨부/   hwp·hwpx·pdf 원본. 온도·적산온도·재해 기준이 여기 있다

feature/          ★ 도구. 검증된 것만 둔다
  hwp_extract.py    hwp/hwpx → txt. 4바이트 형식 판별 · 한글 3.0 johab
  hwpx_table.py     문서에서 표를 **격자로** 읽기 + 등급·부등호 파싱
                    칸 안의 줄바꿈을 살린다 — 안 그러면 한계온도표가 짝을 잃는다
  ocr.py            스캔 PDF (재해예방정보 일부가 이미지)
  clean_text.py     제어문자 제거. 진짜 한자를 지우지 않는 로직 포함
  temperature.py    본문에서 온도값 추출. 문장형까지 잡는다 (372→870건)

pipeline/         ★ 새 파이프라인. 원본 하나에 파일 하나
  crops.py          ★★ 작물 등록표. **새 작물은 여기 한 곳에만** 더한다
  common.py         원본이 달라도 같은 부품 — 글자 다듬기·숫자 읽기·온도 문장 해석
                    ·표 읽기·접기·파일 쓰기 · 중간 CSV 칸(COLS)
                    ⚠ "무엇을 규칙으로 삼을지" 는 여기 넣지 않는다
  src_farmwork.py   농작업일정    ─┐
  src_disaster.py   재해예방정보   │ 원본 하나씩. 수집 스크립트와 이름을 맞추었다
  src_week.py       주간농사정보   │ 따로 돌려도 된다 → out/원본별/<이름>/
  src_pest.py       병해충발생정보  │ ⚠ 접지 않고 날 행을 그대로 쓴다
  src_variety.py    품종정보      ┘   ★ 품종·숙기·생육시기는 여기서만 나온다
                    ⚠ 적산온도·재해 한계온도는 0건이다. mid_sumtemp·mid_rule 이 빈다
                    품종 2,599건은 varieties.csv 로 따로 쓴다 (COLS 밖. build 가 읽는다)
                      숙기 · 숙기가능성 · 숙기근거 · 숙기원문 · 숙기충돌
                      출수기 · 성숙기 · 개화기 · 수확기 · 파종기 · 시기근거
                      용도 · 지대 · 육성년도 · 육성기관 · 첨부형식
  extract.py        원본별 결과를 모아 **한 번만 접고** 쓴다. 판단이 없어 짧다(127줄)
                    ★ 같은 표가 여러 원본에 실려서 실린호를 가로질러 세야 한다
  add_crop.py       작물 하나 되살리기. 보관에서 옮기고 등록줄을 만들어 준다 (사용법 §1.6)
  extract_pest_alert.py  병해충 경보 등급 → out/pest_alert.csv
                    ⚠ 그해 값이라 주 1회 갱신 · 스키마 4종에 자리가 없다
  build.py          중간 CSV + 확정표 상수 → 스키마 4종
  verify.py         빈칸·범위·제약 검사
  사용법.md          돌리는 법 · 나눈 까닭 · 작물 등록법(§1.5) · 원본별 함정
  안읽은것.md        어느 자료를 어디까지 읽었나. "없다" 와 "안 읽었다" 를 가른다
  out/              mid_*.csv → crops · crop_variants · crop_stages · crop_disaster_rules
  out/원본별/        원본별 중간 결과. 날 행(접기 전)이다

GDD계산관련/       ★ 계산 파라미터의 근거
  작물_확정표.md    ★★ 정본. 아래 "정본 규칙" 참고
  GDD_작업인계.md     계산식·기준온도·작물 선정 근거와 실수 기록
  논문/              Paredes et al. (2025) 원문 + 쪽별 txt

기준표/            농업지대.csv  20구분 + 관측지점 60곳
API매뉴얼/         농사로 서비스별 매뉴얼 + 샘플소스 ← 오퍼레이션명은 여기서 확인
보관/             전수 수집 시절의 결과·스크립트·문서. 대조용으로만 연다
```

**.py 와 그 폴더는 영어로 짓는다.** Safe-farm 이 Docker 로 배포되므로 한글 파일명은
로케일이 다른 곳에서 깨진다. 데이터 폴더와 `.md` 는 한글이어도 된다 —
스크립트가 `Path(__file__)` 로 찾으므로 명령줄에 올라오지 않는다.

## 정본 규칙 — 값이 갈릴 때

문서가 여섯 개로 갈라져 같은 값이 서로 다르게 적힌 사고가 있었다. 아래를 지킨다.

```
작물 계산 파라미터의 정본   GDD계산관련/작물_확정표.md
                          §B-2 base_temp·upper_temp  §B-4 저온한계
                          §C 목표 적산온도            §D 숙기
```

- **CSV 와 확정표가 다르면 확정표가 맞다.** CSV 를 확정표에 맞춘다. 반대로 하지 않는다
- **값 → CSV → 문서 순서로 고친다.** 문서를 먼저 고치면 또 앞서 나가 어긋난다
- **결과 문서를 새로 만들면 이 문서(CLAUDE.md)의 파일 구조 절에 같은 작업에서 등록한다.**
  등록 안 된 문서는 다음 사람이 못 찾고, 못 찾으면 같은 걸 또 만든다.
  뿌리에 문서를 늘리기 전에 **기존 문서에 절을 더할 수 없는지 먼저 본다** —
  문서가 여섯 개로 갈라진 게 이번 사고의 원인이다
- 값이 담긴 문서 머리에 정본이 어디인지 한 줄 적는다
- **"확보" 와 "반영" 을 구분해서 적는다.** 자료를 찾은 것과 CSV 에 들어간 것은 다르다.
  "기준온도 12/14 확보" 라고 적어놓고 CSV 에는 4개만 들어 있던 적이 있다

## 작업 방식

**서비스 하나에 폴더 하나.** 실행 스크립트(`.py`) + 사용법(`.md`) + `결과/` 를 한곳에 모은다.

```
<서비스>/
  <이름>.py        실행 스크립트
  사용법.md         사람이 읽는 안내 (옵션·함정·다 받은 뒤 볼 것)
  결과/
    raw/          API 응답 원문 XML   ← 값이 이상할 때 추적용
    첨부/ 또는 파일/  내려받은 원본 문서
    본문/          문서를 글자로 바꾼 txt  ← 실제로 읽는 대상
    *.csv, 요약.md, 대조.md
```

- **`.env` 를 폴더마다 복사하지 않는다.** 스크립트가 상위 폴더로 올라가며 찾게 한다.
  여러 곳에 두면 어느 게 진짜인지 모르게 된다
- **응답 칸을 버리지 않는다.** 안 쓸 것 같은 코드값도 CSV 에 전부 담는다. 판단은 본 뒤에 한다
- **오류 응답을 캐시에 저장하지 않는다.** 저장하면 다음 실행 때 건너뛰어 영영 모르게 된다.
  0바이트로 저장된 파일도 지우고 다시 받는다
- **이어받기.** 중간에 끊겨도 다시 실행하면 이미 받은 것은 건너뛴다
- 요청 사이 `WAIT = 0.4`초. 정부 서버라 몰아서 요청하면 차단될 수 있다
- 오래 걸리는 수집은 `Start-Process` 로 **독립 프로세스**로 띄우고 로그 파일만 확인한다.
  세션의 자식 프로세스로 돌리면 세션이 끝날 때 같이 죽을 수 있다
