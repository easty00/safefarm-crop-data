# 숙기(조생/중생/만생) 데이터 — 실측 확인

2026-09-15 확인. 대상 파일 `품종정보/결과/품종_숙기.csv` (생성 21:59:50).

**확인 요청 내용** : "조생종/중생종/만생종 또는 숙기 데이터가 없다"는 판단이 맞는가.

---

## 결론

**작물을 밝히지 않고 "숙기 데이터가 없다"고 하면 틀립니다.**
**특정 작물(무·파·시금치·당근·가지 등)을 두고 말한 것이면 맞습니다.**

```
품종_숙기.csv        2,599행
숙기가 채워진 행       1,281행 (49%)
숙기가 있는 작물       58종
```

---

## 1. 숙기 데이터는 네 곳에 있습니다

### ① `품종정보/결과/품종_숙기.csv` — 58작물 1,281품종

```
논벼        371품종   중만생 162 · 중생 101 · 조생 98 · 극조생 8 · 만생 2
콩          108품종   중만생 46 · 조생 28 · 중생 22 · 극조생 7 · 만생 5
백합(나리)     69 · 심비디움 51 · 글라디올러스 50 · 감자 46 · 국화 46
들깨         43 · 옥수수 42 · 양파 40 · 프리지어 37 · 배 32 · 고구마 31
보리 23 · 복숭아 23 · 팬지 19 · 감귤 17 · 사과 17 · 단감 15 · 팥 13
밀 12 · 칼라 12 · 참깨 11 · 포도 11 · 꽃도라지 11 · 기장 9 · 조 9 · 땅콩 9 …
```

숙기 출처가 세 갈래로 기록되어 있습니다.

```
코드      427행   varietyInfo 의 숙기 코드(220xxx)
주요특성   693행   mainChartrInfo 문장에서 추출
본문      161행   품종요약서 첨부 본문에서 추출
```

**코드만 보면 427행(16%)입니다.** 이 숫자만 보고 "없다"고 판단하기 쉽습니다.
`safefarm_작물데이터_로드맵.md` §2-2 가 그 16% 를 적어두고
"조생·중생·만생을 코드로는 못 걸러낸다. **본문을 읽어야 한다**" 고 안내하고 있습니다.
본문까지 읽은 결과가 16% → 49% 입니다.

### ② `품종정보/결과/코드표.csv` — 숙기 코드 29개

```
220007 중생종 · 220008 조생종 · 220009 중만생종 · 220010 극조생종
220011 극조숙 · 220012 조숙  · 220013 중숙   · 220014 중만숙 · 220015 만숙
220016~220052 (작물군마다 코드가 따로 부여되어 조생종만 5개 코드)
```

### ③ `재해규칙/결과/disaster_rules.csv` 의 `품종숙기` 칸 — 106행

**재해 판정 기준 자체가 숙기별로 갈려 있습니다.**

```
"조생종 중생종 (만생종)"            36행
"조생종 중생종 (중만생종) 만생종"      17행
"조생종 중생종1 중생종2"             16행
```

사과·배·복숭아의 저온해 위험발령 기준이 숙기마다 다릅니다.

### ④ `농작업일정/결과/본문/논농사/벼(기계이앙) 농작업 일정.txt`

```
벼베기 적기 (출수 후 적산온도가 1,100∼1,200℃ 되는 시기)
지역    조생종   중생종   중만생종
중부     50일    53일    57일
호남     52일    55일    60일
영남     50일    54일    58일
```

---

## 2. 선정 작물 — 원문까지 대조한 결과

| 작물 | 품종정보 품종수 | 숙기 채워짐 | 원문 확인 |
|---|---|---|---|
| 논벼 | 383 | **371** | ✅ |
| 감자 | 46 | **46** | ✅ |
| 양파 | 40 | **40** | ✅ |
| 상추 | 31 | 6 | ✅ "숙기는 중생종이며, 엽색은 진한녹색" |
| 마늘 | 14 | 4 | ✅ "만생으로 숙기가 기존 한지 재래종보다 5일 정도 늦음" |
| 배추 | 11 | 3 | ✅ "소형의 조숙성 구를 형성함" |
| 고추 | 20 | 1 | ✅ |
| 수박 | 20 | 0 | ✅ **맞게 제외** — §3 참조 |
| 토마토 | 21 | 0 | ✅ **맞게 제외** — §3 참조 |
| 무 | 9 | 0 | ✅ 본문 9개 전부에 숙기 표현 없음 |
| 파 | 7 | 0 | ✅ 본문 7개 전부에 없음 |
| 방울토마토 | 5 | 0 | ✅ 본문 5개 전부에 없음 |
| 시금치 | **0** | 0 | ⚠ §4 참조 |
| 당근 | **0** | 0 | ⚠ §4 참조 |
| 가지 | **0** | 0 | ⚠ §4 참조 |

---

## 3. ⚠ 함정 — `조숙재배` 는 숙기가 아니라 작형입니다

수박·토마토 본문에 `조숙` 이라는 낱말이 나옵니다. 그런데 이것은 품종의 숙기가 아닙니다.

```
수박   "<표2> '원예 506호'(11CD15)의 가변특성(2011년 조숙재배)"
토마토  "2002~2003년, 2008년 조숙 및 억제재배에서 특성 및 생산력 검정"
```

**`조숙재배` 는 촉성·반촉성·억제재배와 같은 계열의 작형 이름**입니다.
낱말만 세면 수박 3품종 · 토마토 6품종이 숙기가 있는 것처럼 보이지만 전부 작형입니다.

현재 파서는 이것을 제외하고 있으며 **그 처리가 옳습니다.**

> 이 문서 작성자도 처음에 낱말만 세어 "파서가 수박·토마토를 빠뜨렸다" 고 잘못 지적했다가
> 원문을 확인하고 정정했습니다.

---

## 4. ⚠ "0" 이 세 가지 뜻으로 쓰입니다

같은 0이지만 원인이 다르고, 대응도 다릅니다.

```
① 품종정보에 품종 자체가 없다      시금치 · 당근 · 가지
② 품종은 있는데 숙기 표현이 없다    무 · 파 · 방울토마토
③ 숙기 표현이 작형이라 제외했다     수박 · 토마토
```

### ①이 특히 오해를 부릅니다 — 서비스가 다릅니다

**시금치·당근·가지는 `농작업일정` 에는 전부 있습니다.** 없는 것은 `품종정보` 뿐입니다.

| 작물 | 농작업일정 (farmWorkingPlanNew) | 품종정보 (varietyInfo) |
|---|---|---|
| 시금치 | 작업시기 20행 · 온도 3건 · 본문 1개 | 품종 0 |
| 당근 | 작업시기 31행 · 온도 4건 · 본문 1개 | 품종 0 |
| 가지 | 작업시기 19행 · 온도 6건 · 본문 1개 | 품종 0 |

이 셋에서 이미 값을 뽑아 쓰고 있습니다.

```
시금치   씨뿌림 2·6·9월 / 수확 1~3월·3~7월·10~12월
        표에서 발아 최저 4℃ · 생육 최저 3~4℃      → base_temp 로 채택
당근    봄 3월 · 가을 7월 씨뿌림 / 수확 5~6월 · 10~11월
가지    봄재배(조숙,노지) 5월 정식 → 6~9월 수확
        "17℃ 이하에서는 생육이 정지됨"              → base_temp 후보
```

**즉 "품종정보에 품종이 없다" 는 `crop_variants.maturity_type` 하나에만 영향을 줍니다.**
`crops` 와 `crop_stages` 는 온전히 채울 수 있습니다.

---

## 5. ⚠ 파일이 계속 바뀌고 있었습니다

`품종정보/variety_stage.py` 를 고치는 중이라 결과가 여러 번 재생성되었습니다.
읽은 시점에 따라 값이 달라집니다.

```
              1차      2차       3차(현재)
전체 행수      260   10,147     2,599
상추             0        9         6
수박             0        3         0
토마토            0        5         0
마늘             0        3         4
배추             0        2         3
```

**"없다" 는 판단이 1차 시점의 파일을 읽은 것이라면 그 시점엔 사실이었습니다.**
현재(21:59:50 생성분)가 최종이며, 원문 대조까지 마쳤습니다.

---

## 6. 직접 확인하는 방법

```bash
cd "C:\Users\lecra\Desktop\safe\api테스트"
```

```python
# ① 전체 현황
import csv
from collections import Counter, defaultdict
r = list(csv.DictReader(open("품종정보/결과/품종_숙기.csv", encoding="utf-8-sig")))
print(len(r), "행 · 숙기 채워짐", sum(1 for x in r if x.get("숙기","").strip()))
print("출처:", dict(Counter(x.get("숙기출처","(빈칸)") for x in r)))

d = defaultdict(Counter)
for x in r:
    v = x.get("숙기","").strip()
    if v: d[x["작물명"]][v] += 1
print("숙기 있는 작물:", len(d), "종")
for k, v in sorted(d.items(), key=lambda x: -sum(x[1].values()))[:20]:
    print(f"  {k:12}{sum(v.values()):4}  {dict(v.most_common())}")
```

```python
# ② 특정 작물의 원문에 숙기 표현이 실제로 있는지
import glob, re, os
PAT = re.compile(r"(극조생|조생|중생|중만생|만생|극조숙|조숙|중숙|중만숙|만숙)")
crop = "무"          # 바꿔가며 확인
for p in sorted(glob.glob(f"품종정보/결과/본문/채소/{crop}/*.txt")):
    t = open(p, encoding="utf-8", errors="replace").read()
    m = PAT.search(t)
    if m:
        print(os.path.basename(p), "→", re.sub(r"\s+"," ", t[m.start()-60:m.end()+50]))
```

②를 돌릴 때 **맞은 문장이 작형(`조숙재배`·`억제재배`)인지 품종 숙기인지 눈으로 확인**해야 합니다.

---

## 7. 이 결과로 정한 것

`crop_variants.maturity_type` 은 이렇게 채웁니다.

```
3분할 (EARLY / MID / LATE)
    논벼 371품종 · 감자 46 · 양파 40          근거 충분

MID 한 행
    나머지 11작물                            갈래가 하나이거나 없음
```

접는 규칙

```
극조생 · 조생   → EARLY
중생           → MID
중만생 · 만생   → LATE

논벼로 검산 : EARLY 106 · MID 101 · LATE 164   고르게 나뉨
```

근거 없는 행을 만들지 않고, 나중에 시판 품종 자료가 생기면 행을 추가합니다.

---

## 덧 — 표본의 성격

`품종정보` 는 **농촌진흥청이 육성한 국가 품종** 목록입니다. 시판 종자 시장의
대부분을 차지하는 민간 종자회사(농우바이오·아시아종묘 등) 품종은 들어 있지 않습니다.

```
배추 11품종 · 무 9품종 · 파 7품종
```

시중에 배추 품종이 11개일 리 없습니다. **채소 숙기가 얇은 것은 농진청이 채소를
모본용·저항성 목적으로 육성하는 경우가 많아 숙기 표기를 하지 않기 때문**이기도 합니다.
시판 품종은 "60일 배추", "70일 배추" 처럼 재배일수로 유통됩니다.

→ 자료의 한계이지 파서의 문제가 아닙니다.
