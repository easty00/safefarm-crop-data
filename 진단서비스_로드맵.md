# 병해충 진단 서비스 로드맵 — NCPMS Open API

작성 2026-09-17 · **상태: 설계안. 코드 없음. 수집도 아직 안 했다.**

이 문서의 정본 범위는 **NCPMS(국가농작물병해충관리시스템) API 를 어떻게 쓸 것인가** 뿐이다.
작물 계산 파라미터의 정본은 여전히 `GDD계산관련/작물_확정표.md` 다 — 여기에는 온도값을 적지 않는다.

매뉴얼·샘플은 `npms_api_manual/` (git 추적 안 함). 키는 `.env` 의 `NCPMS_API`.

---

## 0. 한 줄 결론

**된다. 단, NCPMS 에 "사진을 주면 병을 맞혀주는" API 는 없다.**
있는 것은 ① 조건으로 후보를 좁히는 조회형 API 와 ② **농민 사진 + 전문가 진단이 짝지어진 상담 기록**이다.
사진 판독은 우리(비전 모델) 몫이고, NCPMS 는 **후보군을 좁혀주고 방제법 원문을 대주는 역할**이다.

그리고 **1단계에서 이미지 모델 학습은 필요 없다.** 작물·부위로 후보를 10건 안팎까지 줄일 수 있어서
(아래 §1-3 에서 실제로 11건까지 줄었다) 범용 비전 모델 + 후보 대조로 충분히 시작할 수 있다.
학습은 상담 사례가 라벨 데이터로 쌓인 뒤(§6 Phase 3)에 검토한다.

---

## 1. 실호출로 확인한 것 — 2026-09-17

**"확보"와 "반영"을 구분한다**(CLAUDE.md 정본 규칙). 아래는 전부 *확보*, 파이프라인 *반영*은 0건이다.

요청 URL 은 하나다. 서비스는 `serviceCode` 로 갈린다.

```
http://ncpms.rda.go.kr/npmsAPI/service?apiKey=...&serviceCode=SVCxx&serviceType=AA001
                                       serviceType  AA001=XML · AA003=JSON
```

### 1-1. 통합검색 SVC16 — 후보 목록 ✅ 호출 확인

```
요청  cropCode=VC011205 (고추)
응답  totalCount 54 — 병·병원체·해충이 한 목록에 섞여 나온다
      divCode · divName · cropName · korName · oprName(학명) · thumbImg · detailUrl
```

`detailUrl` 은 상세 조회에 쓸 파라미터 문자열을 통째로 준다 (`serviceCode=SVC05&sickKey=D00000208&…`).
**여기서 병/해충이 갈리므로 상세 서비스를 따로 고를 필요가 없다.**

`divCode` — `NP01` 병 · `NP02` 병원체 · `NP03` 해충 · `NP04` 곤충 · `NP05` 천적곤충 · `NP06` 잡초

### 1-2. 병 상세 SVC05 (`sickKey`) ✅ 호출 확인 / 해충 상세 SVC07 (`insectKey`) — 미호출

고추 갈색점무늬병(D00000208)으로 받은 칸:

```
symptoms             증상 (부위별로 줄줄이. <br/> 로 이어진 줄글)
developmentCondition 발생생태 — "여름에서 가을까지 고온,다습한 시기에 많이 발생한다"
preventionMethod     방제법 (경종적)
chemicalPrvnbeMth    약제 방제법   ⚠ 이 건은 **비어 있었다**
virusList            병원체 학명 + 형태 설명
imageList            이미지 + **imageTitle 이 "병든 잎 중기"·"병든 잎 후기" 처럼 병기까지 적혀 있다**
```

⚠ `imageTitle` 이 병기(초기·중기·후기)를 담고 있다 — 비전 대조에 그대로 쓸 수 있는 라벨이다.
⚠ `chemicalPrvnbeMth` 는 빌 수 있다. **비면 "등록약제 정보 없음"으로 낸다. 지어내지 않는다.**

### 1-3. 사진검색 소분류 SVC13 — **부위로 좁히기** ✅ 호출 확인

사용자 안의 3단계("증상을 NCPMS 코드로 가공")에 해당하는 유일한 장치다.

```
요청  cropSectionCode=5 & cropCode=VC011205 & partName=잎
응답  totalCount 11   ← 고추 전체 54건이 11건으로 줄었다
      pestKey · pestName · category(병생태 등) · thumbImg
```

`partName` 은 코드가 아니라 **한글 낱말**이다(URL 인코딩 필요). 받는 값의 전체 목록은 아직 모른다(§7-1).

### 1-4. 상담 검색 SVC41 / 상담 상세 SVC42 ✅ 호출 확인 — **가장 값진 자료**

```
SVC41  cropName=고추  →  totalCount 769 건. 가장 최근 등록 2026-09-15 (어제)
SVC42  dgnssReqNo=8659 →
       cropName 시설고추 · priyClName 잎(주요 발병부위) · reqestCn 농민이 쓴 증상
       answerSe "병해 바이러스" · dbyhs 병해충명 · dgnssOpin 전문가 진단소견
       prvnbeMth 방제법 · imageList 농민 사진 4장 · sidoName/sigunguName 지역 · answrrName 답변자 실명
```

**사진 + 작물 + 부위 + 증상문 + 전문가 진단 + 방제법이 한 건에 다 있다.**
평가셋·few-shot 예시·(나중에) 학습셋으로 이만한 국내 자료가 없다.

⚠ `dbyhs` 가 `알수없음` 인 건이 있다(위 8659). 라벨이 항상 있는 게 아니다 — 세어보고 쓴다.
⚠ 개인정보·저작권이 걸린다. §8 을 먼저 읽을 것.

### 1-5. 코드 표 — `npms_api_manual/4.OpenAPI 코드목록.xlsx`

```
작물코드 cropCode      761개   FC010101 논벼 · VC011205 고추 · VC021001 배추 …
대분류   sKncrCode1     FC 식량작물 · FG 사료녹비 · FL 화훼 · FT 과수 · IC 특용 · VC 채소 · WP 수목
구분코드 divCode        NP01~NP06
예찰코드                벼관찰포·관찰포 14작물
```

---

## 2. 쓸 서비스 다섯 (+ 예비)

| 쓰임 | serviceCode | 열쇠/조건 | 확인 |
|---|---|---|---|
| 후보 조회 | **SVC16** 통합검색 | `cropCode` · `divCode` · `korName` · `searchName` | ✅ |
| 부위로 좁히기 | **SVC13** 사진검색 소분류 | `cropCode` · `partName` · `categoryCode` · `pestName` | ✅ |
| 병 상세·방제 | **SVC05** | `sickKey` | ✅ |
| 해충 상세·방제 | **SVC07** | `insectKey` | ☐ 미호출 |
| 상담 목록 | **SVC41** | `cropName` · `dgnssReqSj` | ✅ |
| 상담 상세 | **SVC42** | `dgnssReqNo` | ✅ |

예비 — 지금은 안 쓴다. `SVC06` 병원체상세 · `SVC08` 곤충상세 · `SVC10` 잡초상세 · `SVC15` 천적곤충상세
· `SVC11/12` 사진검색 대·중분류 · `SVC51` 병해충예찰 · 병해충예측지도(AJAX, REST 아님)

---

## 3. 함정 — 미리 적어둔다

1. ⚠ **매뉴얼이 틀린 곳이 있다.** 4.1.7 통합검색 표에 `serviceCode: SVC01` 이라 적혀 있는데
   실제는 **SVC16** 이다(샘플 jsp 가 맞다). 게다가 **상세 서비스(SVC05·06·07·08·10·15)와
   사진검색(SVC11~13)은 매뉴얼 본문에 레퍼런스가 아예 없다.**
   → **정본은 `npms_api_manual/.../1.병해충검색(사진검색)/1.REST/2.jsp/*.jsp` 샘플이다.**
   농사로에서 "오퍼레이션명은 샘플소스에서 확인한다"는 규칙과 같다. 추측하지 말 것.

2. ⚠ **응답에 우리 apiKey 가 그대로 박혀 돌아온다.** `SVC16` 의 `detailUrl` 이
   `apiKey=<우리키>&serviceCode=SVC05&…` 형태다. **raw 를 저장하기 전에 키를 지운다.**
   (샘플 jsp 에도 남의 실제 키가 박혀 있다. 그걸 쓰지 말 것.)

3. ⚠ `displayCount=3` 을 줬는데 응답이 `5` 였다(SVC41). **요청한 개수가 그대로 오지 않는다.**
   페이지 넘김은 `startPoint` 로 하고 `totalCount` 로 검산한다. 최대 50 · `startPoint` 최대 500 —
   **500 을 넘는 목록은 조건을 쪼개야 한다**(고추 상담만 769건이다).

4. ⚠ **`http` 만 확인했다.** `https` 는 안 해봤다. 농사로(`api.nongsaro.go.kr`)와 **다른 서버·다른 신청**이다.

5. ⚠ **이미지 URL 에 확장자가 없다.** `thumbnailViewer2.mo?...&imageFileName=<긴 토큰>` 꼴이다.
   내려받아 저장할 때는 이름이 아니라 **앞 4바이트로 형식을 판정한다**(CLAUDE.md 문서 파싱 규칙과 같은 이유).

6. ⚠ **작물 이름 축이 셋이고 서로 다르다. 섞지 말 것.**

```
cropSectionCode   1~6        사진검색 전용(식량작물·과수·…)
sKncrCode1/2/3    FC·FC01…   검색 서비스의 대·중·소분류
cropCode          FC010101   작물 하나. 761개
```

7. ⚠ **NCPMS 작물명이 우리 표준명과 다르다.** NCPMS 는 `논벼`(품종정보와 같다),
   상담 데이터는 `시설고추`·`풋고추`처럼 **작형까지 붙어 있다.**
   `cropName` 은 부분일치라 `고추` 로 치면 시설고추가 딸려 온다 — 목록을 넓히는 데는 좋고,
   작물을 세는 데는 나쁘다. **매핑표를 코드에 베끼지 말고 파일로 둔다**(§5).

---

## 4. 진단 흐름 — 검증된 것과 아닌 것

원안 7단계를 실제로 되는 것에 맞춰 고쳐 적는다. **3단계(증상→코드 변환)는 대체로 필요 없다.**
NCPMS 에는 증상 코드 체계가 없고, 받는 조건은 사실상 **작물코드 + 부위 한글낱말 + 이름 검색어**뿐이다.

```
① 사용자가 사진을 찍고 작물을 고른다              작물은 고르게 한다. 맞히려 하지 않는다
② 비전 모델이 사진에서 부위와 증상을 뽑는다        → {부위: 잎, 증상: 갈색 원형 반점, 겹무늬}
       ⚠ 여기만 확률적이다. 나머지는 전부 결정론적 조회다
③ 부위 낱말을 partName 으로 맞춘다                 ⚠ 받는 낱말 목록 미확인 (§7-1)
④ SVC13(cropCode+partName) 으로 후보를 좁힌다      고추+잎 → 11건 ✅ 실측
   비면 SVC16(cropCode) 으로 넓힌다                고추 전체 → 54건 ✅ 실측
⑤ 후보의 symptoms 원문 + 대표이미지를 받아온다     SVC05/SVC07
⑥ 사용자 사진 ↔ 후보(문장+이미지)를 비전 모델로 대조해 순위를 매긴다
⑦ 상위 2~3개를 "추정"으로 보여주고,
   방제법은 preventionMethod · chemicalPrvnbeMth 원문 그대로 붙인다
```

**말투 원칙** — CLAUDE.md 의 "판단은 코드, 문장은 Claude" 에서 ⑥ 만 예외다(모델이 판단한다).
그래서 **확정 어투를 쓰지 않는다.** "○○병입니다" ❌ / "○○병으로 추정됩니다(근거: 잎·갈색 겹무늬 반점)" ⭕
**약제 이름은 절대 지어내지 않는다.** `chemicalPrvnbeMth` 가 비면 비었다고 말한다.
정밀 진단이 필요한 건(바이러스 등)은 상담 신청 경로를 안내한다 — 실제 전문가 답변이 그렇게 한다.

---

## 5. 데이터 층 설계 — 어디에 무엇을 두나

기존 구조를 그대로 따른다(서비스 하나에 폴더 하나 · 원본은 접지 않는다 · 합치기는 build 가 한 번만).

```
원본/병해충진단/                      ★ 새 서비스 폴더
  ncpms.py            수집기. --only 로 서비스별 (검색·상세·상담)
  ncpms사용법.md
  raw/                응답 XML 원문      ⚠ 저장 전 apiKey 문자열 제거 (§3-2)
  이미지/              대표이미지·상담사진  ⚠ 앞 4바이트로 형식 판정
  본문/                상세 줄글을 편 txt

기준표/ncpms_작물매핑.csv             ★ 우리 표준 작물명 ↔ cropCode ↔ NCPMS cropName
                                     사람이 손보는 표. 코드에 베끼지 않는다

pipeline/src_ncpms.py                원본 → 중간 CSV (원본별/ncpms/)
pipeline/out/mid_pest_catalog.csv    작물 · 구분(divCode) · 이름 · 학명 · 키 · 부위 · 이미지
pipeline/out/mid_pest_detail.csv     증상 · 발생생태 · 방제법 · 약제방제법 · 출처키
pipeline/out/ncpms_상담.csv          상담 사례 (스키마 밖. 평가·학습용)
```

스키마로 넘길 것 — **저쪽(Safe-farm ai-service)과 먼저 합의한다. 지금 칸을 정하지 않는다.**

```
후보 ①  crop_docs 에 줄글로 넣는다        ★ 가장 싸다. 청킹·임베딩이 이미 돌고 있다
        증상·발생생태·방제법은 원래 줄글이다. 새 테이블이 필요 없다
후보 ②  pest_catalog 테이블을 새로 만든다  후보 좁히기를 SQL 로 하려면 필요
        → 저쪽 ORM 을 고쳐야 한다. proposal_crop_disaster_rule.py 와 같은 절차
```

⚠ **`out/스키마/` 는 계약이다.** 칸을 늘리면 적재가 깨진다. 합의 전에는 `out/` 안에만 둔다.

**실시간 조회 대상이 아니다** — 마스터 데이터 원칙대로 배포 시 1회 시딩 + 배치 갱신.
다만 **상담 사례는 매일 는다**(어제 등록분이 잡혔다). 갱신 주기는 §7-4.

---

## 6. 단계별 로드맵

### Phase 0 — 확인 (반나절)

- [ ] 이용조건 확인. 공공누리 마크·상담 사진 저작권·재배포 가부 → `출처.txt §6` 에 줄 추가
- [ ] `partName` 이 받는 낱말 전체 목록 확보 (§7-1)
- [ ] SVC07(해충 상세) 한 건 호출해 칸 확인
- [ ] `기준표/ncpms_작물매핑.csv` 초안 — 우리 등록 작물이 761개 cropCode 중 어디에 붙는지
- [ ] `https` · 일일 호출 한도 확인

### Phase 1 — 카탈로그 수집 (하루)

- [ ] `원본/병해충진단/ncpms.py` — 매핑된 작물 전량 SVC16 목록 → SVC05/SVC07 상세 전량
- [ ] 이어받기 · `WAIT=0.4` · **오류 응답을 캐시에 저장하지 않는다** (기존 수집기 규칙 그대로)
- [ ] 대표이미지 내려받기 (썸네일 먼저. 원본은 필요해진 뒤에)
- [ ] 산출: `raw/` + `mid_pest_catalog.csv` + `mid_pest_detail.csv`

### Phase 2 — 반출 결정 (합의 필요)

- [ ] 저쪽과 crop_docs ①인지 새 테이블 ②인지 결정
- [ ] `build.py` 에 붙이고 `verify.py` 에 검사 추가
- [ ] 기존 `crop_docs`(주간농사정보·병해충발생정보)와 **내용이 겹친다.** 중복 청크 정리 방침 필요

### Phase 3 — 상담 사례 (이틀. 이용조건 통과가 전제)

- [ ] SVC41 전량 → SVC42 상세 → 사진
- [ ] ⚠ `startPoint` 500 한도 때문에 작물별·연도별로 쪼개야 한다
- [ ] `dbyhs`(병해충명)가 실제로 채워진 비율을 먼저 센다. 그게 라벨 수율이다
- [ ] 산출: 평가셋(사진↔정답) + few-shot 예시. **개인정보 칸은 분리 보관**

### Phase 4 — 진단 런타임

**이 레포의 일이 아니다.** 비전 호출·순위 매기기는 Safe-farm `ai-service` 쪽이다.
여기서는 **그쪽이 조회할 수 있는 형태로 카탈로그를 넘기는 것까지**가 경계다
(`pipeline/작업기록.md §5` 의 경계와 같은 선).

---

## 7. 미결 — 정해지면 `확인필요.md` 로 옮긴다

1. **`partName` 이 받는 낱말 목록.** '잎'은 됐다. '줄기'·'과실'·'뿌리'·'열매'는 안 해봤다.
   비전 모델이 뽑은 부위를 여기에 맞춰야 하는데, 목록을 모르면 맞출 수가 없다
2. **`cropSectionCode` 를 5 로 넣어 고추가 나왔다.** 이게 맞아서인지 `cropCode` 가 이겨서인지 모른다
3. **`dbyhs` 라벨 수율.** `알수없음` 이 얼마나 되는지 세기 전에는 학습셋이라 부르면 안 된다
4. **갱신 주기.** 카탈로그는 연 1회로 충분하다. 상담은 매일 는다 — 주 1회? 필요한가?
5. **기존 `병해충발생정보`(농사로)와 겹친다.** 농사로 쪽은 *올해 어디서 발생했나*(경보),
   NCPMS 는 *이 병이 무엇인가*(도감)다. **겹치는 게 아니라 축이 다르다** — 합치지 말 것
6. **과수.** NCPMS 는 과수 자료가 두텁다. 재해예방정보 저온해 계열을 안 받은 것과 같은 판단이 걸린다

---

## 8. ⚠ 이용조건·개인정보 — Phase 3 전에 반드시

```
키          .env 의 NCPMS_API. 커밋 금지. npms_api_manual/ 도 git 에서 뺐다
            ⚠ 응답 detailUrl 에 키가 박혀 돌아온다 (§3-2)
농민 사진    상담 사진은 농민이 올린 것이다. 학습·재배포 가부를 확인하기 전에는
            **레포 밖으로 내보내지 않는다**
개인정보     응답에 시도·시군구·읍면동 + 답변자 실명이 들어 있다.
            저장은 하되(추적용) **화면·모델 입력에는 넣지 않는다**
출처 표기    출처.txt §1 에 "농촌진흥청 국가농작물병해충관리시스템(NCPMS)" 절을 더한다.
            농사로와 다른 시스템이다 — 같은 절에 섞어 적지 말 것
```

---

## 9. 이 문서를 고칠 때

- 값(코드·파라미터)을 확인했으면 §1 의 ✅/☐ 를 고친다. **"확보"와 "반영"을 섞어 적지 않는다**
- 새 미결은 §7 에 적고, 정해지면 `확인필요.md` 로 옮긴다
- 수집기·스크립트를 만들면 `CLAUDE.md` 의 파일 구조 절에 **같은 작업에서** 등록한다
